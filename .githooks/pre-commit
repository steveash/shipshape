#!/usr/bin/env bash
# Pre-commit: run the same gate CI runs. Bypass only with --no-verify and a
# reason in the commit body (the gate still catches you in CI).
set -euo pipefail
exec ./scripts/gate.sh --check
